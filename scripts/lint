#!/usr/bin/env bash

set -e

cd "$(dirname "$0")/.."

if [ "$1" = "--fix" ]; then
  echo "==> Running ruff with --fix"
  uv run ruff check . --fix
else
  echo "==> Running ruff"
  uv run ruff check .
fi

echo "==> Running pyright"
uv run pyright -p .

echo "==> Running mypy"
# Use -p together so mypy resolves the editable src layout as `together`
# instead of also discovering it as `src.together` via `mypy .`.
uv run mypy -p together

echo "==> Making sure it imports"
uv run python -c 'import together'
